# ROS2农业UGV项目 - 快速参考卡

## 项目概览
- **ROS2版本**: Humble
- **工作空间**: `/home/orin/Workspace/agri_ugv/ros2_ws`
- **两个平台**: XAG Revo (UDP) + Agilex Hunter (CAN)

---

## 一分钟速查

### 构建命令
```bash
cd /home/orin/Workspace/agri_ugv/ros2_ws
colcon build --symlink-install
source install/setup.bash
```

### Revo启动
```bash
ros2 launch revo_ugv_ros2 revo_bringup.launch.py
```

### Hunter启动
```bash
# 先启动CAN
sudo bash /home/orin/Workspace/agri_ugv/ros2_ws/src/ugv_sdk/scripts/bringup_can2usb_500k.bash

# 再启动Hunter
ros2 launch hunter_base hunter_base.launch.py
```

---

## 常用ROS2命令

### 节点
```bash
ros2 node list                          # 列出所有节点
ros2 node info <node_name>             # 查看节点信息
```

### 话题
```bash
ros2 topic list                         # 列出所有话题
ros2 topic echo <topic>                 # 查看话题数据
ros2 topic hz <topic>                   # 查看发布频率
ros2 topic info <topic>                 # 查看话题信息
ros2 topic pub <topic> <type> <data>    # 发布消息
```

### 消息
```bash
ros2 interface show <message_type>       # 查看消息定义
ros2 interface list                     # 列出所有消息类型
```

### TF
```bash
ros2 run tf2_ros tf2_echo <source_frame> <target_frame>  # 查看TF变换
ros2 run tf2_tools view_frames                                   # 生成TF图
```

---

## 关键话题

### Revo话题
| 话题 | 类型 | 方向 | 说明 |
|------|------|------|------|
| `/revo/pose` | geometry_msgs/PoseStamped | Pub | 底盘位姿 |
| `/revo/system_status` | 自定义消息 | Pub | 系统状态 |
| `/revo/battery` | 自定义消息 | Pub | 电池数据 |
| `/odom` | nav_msgs/Odometry | Pub | 里程计 |
| `/revo/cmd_vel` | geometry_msgs/Twist | Sub | 速度命令 |

### Hunter话题
| 话题 | 类型 | 方向 | 说明 |
|------|------|------|------|
| `/cmd_vel` | geometry_msgs/Twist | Sub | 速度命令 |
| `/odom` | nav_msgs/Odometry | Pub | 里程计 |

---

## 坐标系

### Revo TF树
```
base_footprint (base_footprint)
  └── base_link
        ├── camera (0.4, 0.2, 0.16)
        └── lidar (0.4, 0, 0.2)
```

### Hunter TF树
```
odom
  └── base_link
```

---

## 关键文件路径

### Revo (Python)
```
revo_ugv_ros2/revo_ugv_ros2/keyboard_node.py          ⭐ 主节点
revo_ugv_ros2/revo_ugv_ros2/revo_odom_node.py         ⭐ 里程计
revo_ugv_ros2/launch/revo_bringup.launch.py           ⭐ 启动文件
revo_ugv_ros2/urdf/revo_ugv.urdf.xacro                ⭐ 机器人模型
revosdk/xa_revosdk_ugv/xa_revosdk.py                  ⭐ SDK
```

### Hunter (C++)
```
hunter_ros2/hunter_base/src/hunter_base_node.cpp      ⭐ 主节点
hunter_ros2/hunter_base/launch/hunter_base.launch.py  ⭐ 启动文件
hunter_ros2/hunter_base/urdf/hunter_se.urdf.xacro     ⭐ 机器人模型
ugv_sdk/src/mobile_robot/hunter_robot.cpp             ⭐ SDK
ugv_sdk/scripts/bringup_can2usb_500k.bash             ⭐ CAN启动
```

---

## 调试工具

### rqt_graph - 节点可视化
```bash
rqt_graph
```

### rviz2 - 3D可视化
```bash
rviz2
```

### foxglove - Web调试（Revo）
```
打开浏览器: localhost:8765
```

---

## 常见问题快速解决

### 问题：找不到话题/节点
```bash
# 检查节点是否运行
ros2 node list

# 检查话题
ros2 topic list

# 查看隐藏话题
ros2 topic list --include-hidden-topics
```

### 问题：CAN无法连接
```bash
# 检查CAN接口
ifconfig can0

# 测试CAN通信
candump can0

# 重启CAN
sudo bash .../bringup_can2usb_500k.bash
```

### 问题：UDP无法连接
```bash
# 检查网络
ping 192.168.234.1
```

### 问题：构建失败
```bash
# 清理重建
cd /home/orin/Workspace/agri_ugv/ros2_ws
rm -rf build install log
colcon build --symlink-install --event-handlers console_direct+
```

---

## 测试命令

### 测试速度命令
```bash
# Revo
ros2 topic pub /revo/cmd_vel geometry_msgs/msg/Twist "{linear: {x: 0.5}, angular: {z: 0.0}}"

# Hunter
ros2 topic pub /cmd_vel geometry_msgs/msg/Twist "{linear: {x: 0.5}, angular: {z: 0.0}}"
```

### 查看数据
```bash
# 位姿
ros2 topic echo /revo/pose

# 里程计
ros2 topic echo /odom --field pose

# 系统状态
ros2 topic echo /revo/system_status
```

---

## Python节点模板

```python
import rclpy
from rclpy.node import Node

class MyNode(Node):
    def __init__(self):
        super().__init__('my_node')
        self.publisher = self.create_publisher(String, 'topic', 10)
        self.subscription = self.create_subscription(
            String, 'topic', self.callback, 10)
        self.timer = self.create_timer(0.1, self.timer_callback)

    def callback(self, msg):
        self.get_logger().info(f"收到: {msg.data}")

    def timer_callback(self):
        msg = String()
        msg.data = "Hello"
        self.publisher.publish(msg)

def main():
    rclpy.init()
    node = MyNode()
    rclpy.spin(node)
    rclpy.shutdown()

if __name__ == '__main__':
    main()
```

---

## C++节点模板

```cpp
#include <rclcpp/rclcpp.hpp>
#include <std_msgs/msg/string.hpp>

class MyNode : public rclcpp::Node {
public:
    MyNode() : Node("my_node") {
        publisher_ = this->create_publisher<std_msgs::msg::String>("topic", 10);
        subscription_ = this->create_subscription<std_msgs::msg::String>(
            "topic", 10,
            std::bind(&MyNode::callback, this, std::placeholders::_1)
        );
        timer_ = this->create_wall_timer(
            std::chrono::milliseconds(100),
            std::bind(&MyNode::timer_callback, this)
        );
    }

private:
    void callback(const std_msgs::msg::String::SharedPtr msg) {
        RCLCPP_INFO(this->get_logger(), "收到: %s", msg->data.c_str());
    }

    void timer_callback() {
        auto msg = std_msgs::msg::String();
        msg.data = "Hello";
        publisher_->publish(msg);
    }

    rclcpp::Publisher<std_msgs::msg::String>::SharedPtr publisher_;
    rclcpp::Subscription<std_msgs::msg::String>::SharedPtr subscription_;
    rclcpp::TimerBase::SharedPtr timer_;
};

int main(int argc, char** argv) {
    rclcpp::init(argc, argv);
    auto node = std::make_shared<MyNode>();
    rclcpp::spin(node);
    rclcpp::shutdown();
    return 0;
}
```

---

## 首周任务清单

- [ ] 环境搭建（Day 1）
- [ ] 项目结构理解（Day 1）
- [ ] ROS2基础复习（Day 2）
- [ ] 启动文件阅读（Day 2）
- [ ] Revo代码阅读（Day 3）
- [ ] Revo测试（Day 3）
- [ ] Hunter代码阅读（Day 4）
- [ ] Hunter测试（Day 4）
- [ ] 创建简单节点（Day 5）
- [ ] 编写总结文档（Day 5）

---

## 联系方式
- 技术负责人: [填写]
- 问题反馈: [填写]
- 每日代码审查: [填写时间]

---

**提示**: 打印此卡片，贴在显示器旁边，方便随时查阅！
